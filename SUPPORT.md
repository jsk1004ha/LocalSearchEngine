# Support

## 먼저 확인

- `README.md`의 설치/설정 섹션
- `TROUBLESHOOTING.md`

## 문의/버그 리포트에 포함하면 좋은 정보

- OS / Python 버전 (`python --version`)
- 실행 명령어와 옵션(`--mode`, `--time-budget`, `--web-fetch` 등)
- 관련 환경 변수(`PYTHONPATH`, `INVESTIGATION_SEARCH_AUTO_INSTALL`, `INVESTIGATION_USE_PLAYWRIGHT_FETCH`, `OLLAMA_BASE_URL`)
- 에러 로그(가능하면 전체)와 재현 단계

## 보안 이슈

보안 관련 내용은 `SECURITY.md`를 따라 주세요.

